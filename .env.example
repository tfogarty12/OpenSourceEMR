# Copy to .env for local development. NEVER commit a real .env or any PHI.

# API
HOST=0.0.0.0
PORT=8080
LOG_LEVEL=info

# PostgreSQL (matches docker-compose.yml dev defaults)
DATABASE_URL=postgres://osemr:osemr_dev_only@localhost:5432/osemr
