# Use a strict, reproducible install.
engine-strict=true
# Keep the dependency tree explicit; no phantom (hoisted) dependencies.
# This matters in a clinical codebase where supply-chain clarity is a safety property.
shamefully-hoist=false
